# This workflow is intentionally disabled.
# The deploy.yml workflow handles building and deploying this React/Vite app.
name: Disabled - use deploy.yml

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use deploy.yml instead.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See deploy.yml."
